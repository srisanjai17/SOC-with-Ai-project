# ═══════════════════════════════════════════════════════════════════
# SOC with AI — Development Dependencies
# Install with the rest of the toolchain:
#     python scripts/setup_dev.py
# Or manually:
#     pip install -r requirements.txt -r requirements-dev.txt
# ═══════════════════════════════════════════════════════════════════

# Linting
ruff>=0.4.0
yamllint>=1.33.0

# Unit / integration testing
pytest>=7.4.0
pytest-cov>=4.1.0
httpx>=0.25.0          # TestClient transport

# Browser E2E testing (Chromium is installed separately)
playwright>=1.40.0
